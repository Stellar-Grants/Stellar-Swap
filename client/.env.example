# Base URL for the Stellar-Swap API server
# Default for local development:
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Stellar network for explorer links: "testnet" or "mainnet" (default: testnet)
NEXT_PUBLIC_STELLAR_NETWORK=testnet
